import ProductPage from "@/components/products/Products";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <ProductPage />
    </div>
  );
};

export default page;
